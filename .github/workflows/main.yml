name: Cat Facts  

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  POST:

    runs-on: ubuntu-latest
    
    steps:
    - name: Grant execute permission for gradlew
      run: curl --location 'https://cat-fact.herokuapp.com/facts/'
